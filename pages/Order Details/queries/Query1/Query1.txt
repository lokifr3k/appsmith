-- Erase it from the orders table
UPDATE export_orders 
SET folding_type = '' 
WHERE folding_type = '';

-- Erase it from the item master table
UPDATE item_masters 
SET folding_type = '' 
WHERE folding_type = 'Standard';